func=lambda x, y: len(xList := x.strip().split("\n")) == len(yList := y.strip().split("\n")) and len(xList) > 1 and len(yList) > 1 and int(xList[0]) == int(yList[0]) and set(map(int,yList[1].split())) == set(map(int, xList[1].split()))	input=input.txt